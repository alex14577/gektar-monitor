"""Diagnostics sub-package.

Currently exposes:
  - DiagnosticsExcludePolicy: PII exclude / redaction rules for diagnostic.zip.

DiagnosticsService (a4t.7) will live here once implemented and will consume
DiagnosticsExcludePolicy.
"""
